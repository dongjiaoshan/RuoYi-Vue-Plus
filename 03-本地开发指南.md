# 东角山 — 本地启动指南

> 目标：**1 小时跑通**（环境 + 后端 + 前端登录）。
> 平台：macOS（Apple Silicon）。后端 IDEA + 前端 pnpm + 数据栈 Docker。
> 业务范围：**MySQL + Redis + MinIO/OSS + 工作流 + 短信**；**不开** snail-job 定时任务。

---

## 1. 必装环境

| 组件 | 版本 |
|---|---|
| JDK | **21** OpenJDK（不要 Oracle）|
| Maven | ≥ 3.8 |
| Node.js | ≥ 20.15 |
| pnpm | latest |
| Docker Desktop | latest |

```bash
brew install openjdk@17 maven node
npm i -g pnpm

# JDK 17 写入 PATH
echo 'export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"' >> ~/.zshrc
echo 'export JAVA_HOME="/opt/homebrew/opt/openjdk@17"' >> ~/.zshrc
source ~/.zshrc
java -version    # openjdk version "17.x.x"
```

> 装过其他 JDK 的：用 [jenv](https://github.com/jenv/jenv) 切到 17。

### 国内镜像（必做，否则 Maven/npm 卡死）

```bash
# npm
npm config set registry https://registry.npmmirror.com
pnpm config set registry https://registry.npmmirror.com

# Maven —— 写到 ~/.m2/settings.xml
mkdir -p ~/.m2 && cat > ~/.m2/settings.xml <<'EOF'
<settings>
  <mirrors>
    <mirror>
      <id>aliyun</id>
      <url>https://maven.aliyun.com/repository/public</url>
      <mirrorOf>central</mirrorOf>
    </mirror>
  </mirrors>
</settings>
EOF
```

---

## 2. Docker 数据栈

不要用项目自带的 `script/docker/docker-compose.yml`（生产配置，`network_mode: host` 在 Mac 上不工作）。用下面的轻量版：

具体已写入到 `script/docker/docker-compose-dev.yml`

```bash
cat > ~/dev-stack-docker-compose.yml <<'EOF'
services:
  mysql:
    image: mysql:8.0
    container_name: dev-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ry-vue
      TZ: Asia/Shanghai
    ports: ["3306:3306"]
    command:
      - --default-authentication-plugin=mysql_native_password
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_general_ci
      - --lower_case_table_names=1
    volumes: [dev-mysql-data:/var/lib/mysql]

  redis:
    image: redis:7
    container_name: dev-redis
    ports: ["6379:6379"]
    command: redis-server --requirepass "ruoyi123"
    volumes: [dev-redis-data:/data]

  minio:
    image: minio/minio:latest
    container_name: dev-minio
    environment:
      MINIO_ROOT_USER: ruoyi
      MINIO_ROOT_PASSWORD: ruoyi123
    ports: ["9000:9000", "9001:9001"]
    command: server /data --console-address ":9001"
    volumes: [dev-minio-data:/data]

volumes:
  dev-mysql-data:
  dev-redis-data:
  dev-minio-data:
EOF

docker compose -f ~/dev-stack-docker-compose.yml up -d
docker ps    # 看到 dev-mysql / dev-redis / dev-minio 都 Up
```

**默认账号**（已与项目 `application-dev.yml` 对齐，**不要改密码**）：
- MySQL：`root / root`，库 `ry-vue`
- Redis：密码 `ruoyi123`
- MinIO 控制台 `http://localhost:9001`：`ruoyi / ruoyi123`

---

## 3. 导 SQL（顺序敏感）

```bash
cd code/main/RuoYi-Vue-Plus/script/sql

docker exec -i dev-mysql mysql -uroot -proot ry-vue < ry_vue_5.X.sql
docker exec -i dev-mysql mysql -uroot -proot ry-vue < ry_workflow.sql

# 验证
docker exec -i dev-mysql mysql -uroot -proot ry-vue -e "SHOW TABLES;" | wc -l
# 应该 35 左右（核心 23 张 + workflow 11 张）

# 验证中文不乱码
docker exec -i dev-mysql mysql -uroot -proot ry-vue \
  -e "SELECT user_name, nick_name FROM sys_user LIMIT 1;"
# 看到 "疯狂的狮子Li" = OK；看到 "?????Li" = 字符集挂了，重建容器
```

**跳过 `ry_job.sql`**（snail-job 定时任务本期不用）。

---

## 4. 改一处配置

`code/main/RuoYi-Vue-Plus/ruoyi-admin/src/main/resources/application-dev.yml`：

```yaml
snail-job:
  enabled: false   # ← 改成 false，不然启动连不上 17888 报错
```

数据库、Redis、MinIO 配置默认值已经跟上面 Docker 一致，不用动。

---

## 5. 后端启动（IDEA）

1. IDEA 打开 `code/main/RuoYi-Vue-Plus/`（**不要打开外层 `main/`**，会把前端 ts 当 Java 索引）
2. 必装插件：Lombok、MyBatisX
3. 等 Maven 拉依赖（首次 5-10 分钟）
4. Run `ruoyi-admin/src/main/java/org/dromara/DromaraApplication`
5. 看到 `Started DromaraApplication in X seconds` = OK，API 在 `http://localhost:8080`

> Spring Boot DevTools 已引入，改完代码 `Cmd+F9` Build Project 触发 5-10s 热重启。

---

## 6. 前端启动（pnpm）

```bash
cd code/main/plus-ui
pnpm install

# 改默认端口 80 → 8000（80 端口要 root）
# 编辑 .env.development，VITE_APP_PORT=8000

pnpm run dev
```

浏览器开 `http://localhost:8000`，登录 `admin / admin123`。

---

## 7. 验证（4 步全通 = 环境 OK）

1. `http://localhost:8080/doc.html` 看到 API 列表
2. `http://localhost:8000` 用 `admin/admin123` 登录进首页
3. 左侧菜单加载出来（动态路由 `/getRouters` 通）
4. 点 **系统管理 → 用户管理**，能看到用户列表

---

## 8. 日常开发要点

**新增 CRUD**：登录 → 系统工具 → 代码生成 → 导入表 → 编辑配置 → 生成 zip → 解压回项目对应模块。**不要手写 CRUD 三件套**，5-15 分钟搞定。

**权限三处必须一致**：
- 后端方法 `@SaCheckPermission("module:resource:action")`
- 前端按钮 `v-hasPermi="['module:resource:action']"`
- 数据库 `sys_menu` 表权限标识

**前端热重载**：Vite HMR 自动刷新，无需重启。
**后端热重载**：IDEA `Cmd+F9` Build Project 触发 DevTools 重启。

**MinIO 第一次用要建 bucket**：`http://localhost:9001` 登录 → Buckets → Create `ruoyi`（公开）。

---

## 9. 常用命令

### Docker
```bash
docker compose -f ~/dev-stack-docker-compose.yml up -d     # 起
docker compose -f ~/dev-stack-docker-compose.yml down      # 停
docker exec -it dev-mysql mysql -uroot -proot ry-vue       # 进 MySQL
docker exec -it dev-redis redis-cli -a ruoyi123            # 进 Redis
docker compose -f ~/dev-stack-docker-compose.yml down -v   # 重置（删数据）
```

### 后端 Maven
```bash
mvn clean install -DskipTests                              # 全量编译
mvn -pl ruoyi-modules/ruoyi-system -am clean install -DskipTests   # 单模块
mvn -pl ruoyi-admin dependency:tree                        # 依赖树
mvn clean install -U -DskipTests                           # 强刷依赖
```

### 前端 pnpm
```bash
pnpm run dev                                               # 启动
pnpm run build:prod                                        # 生产构建
pnpm run lint:eslint:fix                                   # Lint 自动修
rm -rf node_modules && pnpm install                        # 清装
```

---

## 10. 故障排查

| 症状 | 解决 |
|---|---|
| 后端启动连不上 17888 | 改 `snail-job.enabled: false`（§4）|
| `Communications link failure` | `docker ps` 看 MySQL 状态；端口 3306 是否被占 |
| `Could not connect to Redis` | Redis 容器没起 / 密码不是 `ruoyi123` |
| `Table 'ry-vue.sys_xxx' doesn't exist` | SQL 没导全，重导 `ry_vue_5.X.sql + ry_workflow.sql` |
| 前端登录"客户端ID不正确" | `.env.development` 的 `VITE_APP_CLIENT_ID` 跟 `sys_client` 表对不上 |
| 接口 401 | F12 看请求 header；RSA 公钥跟 `sys_client.client_secret` 私钥配对 |
| `mvn install` 拉依赖失败 | 检查 `~/.m2/settings.xml` 阿里云镜像 |
| MyBatis XML 改完不生效 | IDEA Build Project (`Cmd+F9`) 触发重启 |
| Lombok 注解报红 | Settings → Build → Compiler → Annotation Processors → Enable |
| Druid SQL 监控 | `http://localhost:8080/druid`，账号 `ruoyi/123456` |
| Redis 缓存查看 | `docker exec -it dev-redis redis-cli -a ruoyi123` 然后 `KEYS *` |

---

## 11. Git 策略

后端 + 前端是**两个独立 git 仓库**（fork 自上游 dromara，非 submodule）。

- **第一次拉锁定 tag**：`git checkout v5.x.x`，不追上游 main
- 业务代码 commit 到自己 fork 的 dev 分支
- 上游有重大修复才合：`git remote add upstream ... && git fetch upstream && git merge upstream/v5.x.x`

顶层 `dongjiaoshan/` 目录不加 git（文档和代码生命周期不同）。

---

## 12. 阿里云采购清单

**详见独立文档**：[04-阿里云采购清单.md](04-阿里云采购清单.md)

含双环境对照、详细规格、官网真实价格调研、付费方式建议、购买顺序时间线。

### 12.1 配置切换速查（本地 vs 测试 vs 生产）

| 服务 | 本地 | 测试 | 生产 |
|---|---|---|---|
| MySQL | Docker | RDS 内网地址 | RDS 高可用内网地址 |
| Redis | Docker | Redis 内网地址 | Redis 主从内网地址 |
| 文件存储 | MinIO `:9000` | OSS endpoint | OSS endpoint（同 region 内网）|
| 短信 | mock / 日志 | SMS 真发（少量）| SMS 真发 |

切换方式：分别维护 `application-dev.yml` / `application-test.yml` / `application-prod.yml`，启动加 `--spring.profiles.active=test|prod`。**生产密码不进 git**，用阿里云配置中心 ACM 或环境变量注入。
---

## 附：第一天最短路径

```bash
# 1. 装环境（30 分钟）
brew install openjdk@17 maven node && npm i -g pnpm
# 配 PATH + 国内镜像（§1）

# 2. 起 Docker（5 分钟）
# 写 ~/dev-stack-docker-compose.yml（§2）
docker compose -f ~/dev-stack-docker-compose.yml up -d

# 3. 导 SQL（2 分钟）
cd code/main/RuoYi-Vue-Plus/script/sql
docker exec -i dev-mysql mysql -uroot -proot ry-vue < ry_vue_5.X.sql
docker exec -i dev-mysql mysql -uroot -proot ry-vue < ry_workflow.sql

# 4. 改 application-dev.yml：snail-job.enabled = false（1 分钟）

# 5. IDEA 打开 RuoYi-Vue-Plus，Run DromaraApplication（首次 10 分钟拉依赖）

# 6. 前端（5 分钟）
cd code/main/plus-ui
pnpm install
# 改 .env.development: VITE_APP_PORT=8000
pnpm run dev

# 7. 浏览器 http://localhost:8000 → admin/admin123
```
