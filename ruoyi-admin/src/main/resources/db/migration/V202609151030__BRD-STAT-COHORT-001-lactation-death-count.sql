-- 断奶登记补「本窝哺乳期死淘数」（BRD-STAT-COHORT-001 / D-0065）。
--
-- 产房损失率恒 0 的根因在录入侧：mp 断奶表单的断奶头数是只读汇总（= 该窝已贴标仔猪行数，或按活产仔数
-- 铺的 N 行），工人没有减头的交互，于是 weaned_count 恒等于 live_born，任何「活仔 − 断奶」的损失率都是 0。
-- 甲方 2026-07-09（旧表 小程序:28）已明确「产房损失率 0.00% 取值明显不合理」。
--
-- 为什么用一个纯数字字段而不是逐头标记（Kevin 2026-09-14 拍板方案 c）：
-- 生产上绝大多数哺乳仔猪没打耳标（实测 9/9 当天出生 5 窝 48 头、个体档案 0 条），逐头删行要联动状态机
-- 记 DIE，但没有档案的猪根本没有「头」可标；为落不了地的精度付整套联动的代价不划算。窝级一个数字即可
-- 回答甲方要的「损失有个归属」。
--
-- 损失率分子直接取本列而不是 live_born − weaned_count：贴标率不足时铺行数 < 活仔数，
-- 差值里会混进「没贴标所以没进断奶清单」的头，间接算法会把它误判成死亡。
ALTER TABLE t_farm_pig_weaning
    ADD COLUMN lactation_death_count INT NOT NULL DEFAULT 0
    COMMENT '本窝哺乳期死淘数（出生后到断奶期间死亡/淘汰的仔猪头数，产房损失率分子）' AFTER weaned_count;
