-- 门店退回：仓库确认时区分「退回入库」与「产品丢弃」
-- 小程序 行269：退回确认页每个产品可切「退回入库（默认，红字）」⇄「产品丢弃（灰字）」；
--              仅退回入库才写入库记录进库存，丢弃不入库、只记为丢弃。
-- admin 行203：仓库退回记录的「确认进度」由「已确认/总数」改为「入库数/丢弃数」。
-- admin 行204：退回明细弹窗在差异量右侧加「是否丢弃」列。
--
-- 放在 t_store_return（扁平单表，一行=一个产品的一条退回）上：丢弃是**行级**决定，
-- 甲方截图里横线也画在单个产品卡内部，不是整单开关。

ALTER TABLE t_store_return
    ADD COLUMN is_discard TINYINT NOT NULL DEFAULT 0
    COMMENT '仓库确认处置：0=退回入库(默认，写库存) / 1=产品丢弃(不入库)' AFTER received_weight;
